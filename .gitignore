# dependencies
node_modules/
.pnp
.pnp.*

# builds
dist/
build/
out/
.next/
*.tsbuildinfo
next-env.d.ts

# tests / coverage
coverage/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# env
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# editors / os
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.DS_Store
Thumbs.db

# misc
.vercel
*.pem

# внешние наборы скилов: содержимое не версионируется, как node_modules. Источник, ветка, коммит
# и treeHash каждого набора лежат в `skills-lock.json` — он в git. Восстановление и сверка:
# `pnpm skills:sync` / `pnpm skills:check`. Адаптеры под этот репозиторий живут в `.claude/skills/`.
.agents/
